// Here you generate *JavaScript* that would be executed in the browser

function refreshForm(innerHTML) {
  const newReviewForm = document.getElementById('new_review');
  newReviewForm.innerHTML = innerHTML;
}

function addReview(reviewHTML) {
  const reviews = document.getElementById('reviews');
  reviews.innerHTML = reviewHTML;
}

<% if @review.errors.any? %>
  refreshForm('<%= j render "reviews/form", lama: @lama, review: @review %>');
<% else %>
  addReview('<%= j render "reviews/show", reviews: @reviews %>');
  refreshForm('<%= j render "reviews/form", lama: @lama, review: Review.new %>');
<% end %>
